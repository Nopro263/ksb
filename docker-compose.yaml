services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: root
    volumes:
      - ./data:/var/lib/postgresql/data
    ports:
      - 5432:5432
 
  adminer:
    image: adminer
    ports:
      - 8080:8080
